version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    command: redis-server --save 60 1 --loglevel warning

  # Optional MinIO for object storage if needed later, but using local fs for MVP default as per spec
  # minio:
  #   image: minio/minio
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   command: server /data --console-address ":9001"

  # We will run apps locally via `npm run dev` for the "local dev runs with one command" requirement,
  # but here is a placeholder for a full containerized stack if the user wants "production build".
